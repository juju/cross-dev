images:
  # LTS
  juju-3.6:
    dockerfile: Dockerfile
    context: context
    registry_paths:
      - ghcr.io/juju/cross-dev
    tags:
      - juju-3.6
    platforms:
      - linux/amd64
    build_args:
      - "BASE_IMAGE=public.ecr.aws/ubuntu/ubuntu:24.04"
      - "GOVERSION=1.22"
